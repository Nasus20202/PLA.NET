<UserControl
  x:Class="ProcessMonitor.UserControls.ProcessDetailsControl"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="450"
  d:DesignWidth="800"
  x:Name="processDetailsControl"
>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <!-- Process Details -->
    <GroupBox Header="Scalar Properties" Grid.Row="0" Margin="5">
      <Grid Margin="5">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Process ID:" Grid.Row="0" Grid.Column="0" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding Id}" Grid.Row="0" Grid.Column="1" Margin="5" />
        <TextBlock Text="Name:" Grid.Row="0" Grid.Column="2" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding Name}" Grid.Row="0" Grid.Column="3" Margin="5" />
        <TextBlock Text="Working Set:" Grid.Row="1" Grid.Column="0" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding WorkingSetMB}" Grid.Row="1" Grid.Column="1" Margin="5" />
        <TextBlock
          Text="Private Memory:"
          Grid.Row="1"
          Grid.Column="2"
          Margin="5"
          FontWeight="Bold"
        />
        <TextBlock Text="{Binding PrivateMemoryMB}" Grid.Row="1" Grid.Column="3" Margin="5" />
        <TextBlock Text="Priority:" Grid.Row="2" Grid.Column="0" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding Priority}" Grid.Row="2" Grid.Column="1" Margin="5" />
        <TextBlock Text="Threads:" Grid.Row="2" Grid.Column="2" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding ThreadCount}" Grid.Row="2" Grid.Column="3" Margin="5" />
        <TextBlock Text="Handles:" Grid.Row="3" Grid.Column="0" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding HandleCount}" Grid.Row="3" Grid.Column="1" Margin="5" />
        <TextBlock Text="Start Time:" Grid.Row="3" Grid.Column="2" Margin="5" FontWeight="Bold" />
        <TextBlock Text="{Binding StartTime}" Grid.Row="3" Grid.Column="3" Margin="5" />
        <TextBlock Text="Window Title:" Grid.Row="4" Grid.Column="0" Margin="5" FontWeight="Bold" />
        <TextBlock
          Text="{Binding MainWindowTitle}"
          Grid.Row="4"
          Grid.Column="1"
          Grid.ColumnSpan="3"
          Margin="5"
          TextTrimming="CharacterEllipsis"
        />
      </Grid>
    </GroupBox>
    <!-- Threads and Modules (Lists) -->
    <TabControl Grid.Row="1" Margin="5">
      <TabItem Header="Threads">
        <DataGrid
          ItemsSource="{Binding ElementName=processDetailsControl, Path=Threads}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          SelectionMode="Single"
        >
          <DataGrid.Columns>
            <DataGridTextColumn Header="Thread ID" Binding="{Binding Id}" Width="100" />
            <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" Width="150" />
            <DataGridTextColumn
              Header="Priority Level"
              Binding="{Binding PriorityLevel}"
              Width="120"
            />
            <DataGridTextColumn Header="State" Binding="{Binding ThreadState}" Width="100" />
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>
      <TabItem Header="Modules">
        <DataGrid
          ItemsSource="{Binding ElementName=processDetailsControl, Path=Modules}"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          SelectionMode="Single"
        >
          <DataGrid.Columns>
            <DataGridTextColumn Header="Module Name" Binding="{Binding ModuleName}" Width="200" />
            <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="*" />
            <DataGridTextColumn Header="Base Address" Binding="{Binding BaseAddress}" Width="120" />
            <DataGridTextColumn Header="Size" Binding="{Binding ModuleMemorySize}" Width="100" />
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>
    </TabControl>
  </Grid>
</UserControl>
